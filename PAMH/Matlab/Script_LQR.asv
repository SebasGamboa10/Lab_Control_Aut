% --> LQR <---

A = [-0.1456 4.141; -4.141 -0.1456];
B = [0;1.966];
C = [1.388;];
D = [0 0;0 0];

Q = [200 0 0 0 0 0; 0 150 0 0 0 0; 0 0 100 0 0 0; 0 0 0 200 0 0; 0 0 0 0 50 0; 0 0 0 0 0 50];
R = [1 0; 0 1];

%K = lqr(A,B,Q,R);

%ceros es matriz de ceros
ceros=zeros(length(A),2); %El numero de columnas de la matriz de ceros

As = [A ceros; -C D];
Bs = [B; D];

Ks = lqr(As,Bs,Q,R);

%Siempre hay que analizar el caso
K = Ks(:,[1:4])
Ki = -Ks(:,[5:6])


